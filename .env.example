# Local development env. In Swarm these come from Docker secrets (see stack.yaml).
# Copy to .env and fill in. NEVER commit .env.

PORT=3000

# Supabase project ref. Used as the pooler username suffix (<role>.<ref>).
PROJECT_REF=YOUR_PROJECT_REF

# IPv4-compatible Supavisor pooler host (Connect -> Transaction pooler in the
# dashboard). The direct host db.<ref>.supabase.co is IPv6-only.
MCP_DB_HOST=aws-1-YOUR_REGION.pooler.supabase.com

# 32-byte AES master key as 64 hex chars. Generate: openssl rand -hex 32
# Must match the key used by scripts/provision-token.ts.
MCP_MASTER_KEY=

# Control connection for the mcp_resolver role (reads only internal.mcp_api_tokens).
# Pooler format: <role>.<ref>@<pooler-host>:6543 (transaction mode).
MCP_RESOLVER_CONN=postgresql://mcp_resolver.YOUR_PROJECT_REF:SENHA@aws-1-YOUR_REGION.pooler.supabase.com:6543/postgres?sslmode=require

# Defense-in-depth statement guard (true|false). Default true.
MCP_STATEMENT_GUARD=true

# Resolver cache TTL in ms (also the max revocation delay). Default 60000.
MCP_CACHE_TTL_MS=60000
